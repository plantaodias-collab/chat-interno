PORT=3000
SECRET_KEY=troque-por-uma-chave-forte

# Opcional: controla onde data/ e uploads/ ficam salvos.
# Em Railway, o app usa automaticamente RAILWAY_VOLUME_MOUNT_PATH se existir.
# STORAGE_ROOT=/app/storage

# Opcional: caminhos separados se quiser customizar.
# DATA_DIR=/app/storage/data
# UPLOAD_DIR=/app/storage/uploads

# Bootstrap automatico do primeiro admin.
# So sera usado se ainda nao existir nenhum admin ativo.
ADMIN_NOME=Administrador
ADMIN_EMAIL=admin@empresa.com
ADMIN_SENHA=TroqueEssaSenha123!
