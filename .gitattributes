# Snapshots normativos oficiais: preservar bytes e whitespace de origem,
# pois o manifesto valida o SHA-256 do conteúdo adquirido.
resources/normative/*.html -text whitespace=-trailing-space,-space-before-tab
